#include <iostream>
using namespace std;

int dwarf[9]; // 아홉 난쟁이 모자에 쓰여있는 수

int GetSum()
{
	int sum = 0; // 아홉 수를 다 더한 값
	for (int i = 0; i < 9; ++i) sum += dwarf[i];

	return sum;
}

int main() {
	ios::sync_with_stdio(0);
	cin.tie(NULL); cout.tie(NULL);

	int sum; // 아홉 수를 다 더한 값

	for (int i = 0; i < 9; ++i) cin >> dwarf[i];

	sum = GetSum();

  /* 전체 합에서 두 수를 선택하여 빼는 방법 */
	for (int i = 0; i < 8; ++i)
	{
		for (int j = i + 1; j < 9; ++j)
		{
			if (sum - dwarf[i] - dwarf[j] == 100)
			{
				for (int x = 0; x < 9; ++x)
					if (x != i && x != j) cout << dwarf[x] << '\n';
				return 0;
			}
		}
	}

}
